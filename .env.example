# Environment Configuration
# Copy this file to .env.local for local development

# =============================================================================
# Test Data Configuration
# =============================================================================

# Whether to use test/demo data instead of real database
# Set to "true" for development/demo mode, "false" for production
NEXT_PUBLIC_USE_TEST_DATA=true

# =============================================================================
# Supabase Configuration (for production)
# =============================================================================

# Supabase Project URL
# Get this from your Supabase project settings
# Example: https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# Supabase Anonymous Key (public/anon key)
# Get this from your Supabase project settings > API
# This key is safe to use in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# =============================================================================
# Notes
# =============================================================================

# For local development:
# 1. Copy this file to .env.local
# 2. Set NEXT_PUBLIC_USE_TEST_DATA=true to use mock data
# 3. Leave Supabase variables empty

# For production deployment:
# 1. Set NEXT_PUBLIC_USE_TEST_DATA=false
# 2. Add your Supabase URL and ANON_KEY
# 3. Ensure your Supabase database is set up with the schema from 20250412_initial_schema.sql

# Security Notes:
# - Never commit .env.local to version control (it's in .gitignore)
# - Only NEXT_PUBLIC_* variables are exposed to the browser
# - The ANON_KEY is safe to expose (RLS policies protect your data)
# - Never expose SUPABASE_SERVICE_ROLE_KEY to the browser
